// Exporting an object of UserActionTypes
export const UserActionTypes = {
	// A value that points to the exact string in user.actions and user.reducer
	// Now it's constant and we don't have to think about typos
	SET_CURRENT_USER: 'SET_CURRENT_USER'
}